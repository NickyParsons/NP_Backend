@page "/app"
@model TestAspNetApplication.Pages.AppModel
<component type="typeof(BlazorApp)" render-mode="ServerPrerendered"></component>
@{
    // await Html.RenderComponentAsync<Counter>(RenderMode.ServerPrerendered);
    ViewBag.Title = "Эта страница инъектирует компоненты Razor";
}
